import AwokenRef from './src'

//console.log(AwokenRef.parseOrThrow('Gen 1 - Exo 2,3,4:1-2'));
console.log(AwokenRef.parseOrThrow('Ketuvim'))
//console.log(AwokenRef.parseOrThrow('Exo2:4,5'))

/*for(let b of AwokenRef.versification.order) {
	console.log(AwokenRef.format(AwokenRef.makeRange(b.id).end, "url"));
}*/
